package com.osh.utils;

public class ObservableDouble extends ObservableItemBase<Double> implements IObservableDouble {
    public ObservableDouble(Double initialValue) {
        super(initialValue);
    }
}
